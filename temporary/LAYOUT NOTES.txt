collection.users {
	doc.user {
		colllection.stats {
			doc.totalTime
			doc.totalSessions
			doc.averageDuration
		}

    collection.friends{
    }
			
		collection.pet {
			doc.name
			doc.level
			doc.totalExp
			doc.height
			?doc.happiness
		}
	}
}
	

start-task.html
	USER
  1a. user submits time they want and timer begins.
	1b. stats.totalSessions++
	2a. time is STOPPED or HITS ZERO.
	2b. stats.totalTime += intiialTime - remainingDuration
	2c. stats.averageDuration = stats.totalSessions / stats.totalTime
  
  PET
  3a. exp is REWARDED upon a COMPLETED, NON-STOPPED task.
  3b. pet.exp += 6 * sqrt(5(initialTime-10)) //eg. 30 min = 60 exp, 60 min = 100 exp
  4a. level is CALCULATED based on totalExp.
  4b. level = totalExp / 100
  4c. displayExp = totalExp % 100 //eg. totalExp = 124, it'll show 24/100 exp till next level.
  5a. heigh is calculated based on level.
  5b. height = 0.1 * 1.05 ^ level